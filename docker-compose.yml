services:
  app:
    build: .
    container_name: go-blog-app
    restart: always
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      SESSION_KEY: ${SESSION_KEY}
    env_file:
      - .env 